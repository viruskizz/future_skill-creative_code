let txt;
let button;
let isHide = false;
function setup() {
  const canvas = createCanvas(400, 400);
  let txt = createDiv('This is an HTML string!');
  button = createButton('click');
}

function draw() {
  background(220);
}
